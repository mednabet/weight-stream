# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=production_manager
DB_USER=prod_app
DB_PASSWORD=your_password_here

# Server
PORT=3001
# Comma-separated list of allowed origins (use your production domain, never use *)
CORS_ORIGIN=http://localhost:5173,https://yourdomain.com

# Security
JWT_SECRET=your_jwt_secret_here_change_in_production
